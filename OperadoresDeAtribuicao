#2 Saldo e deposito
saldo = float(input('Informe o saldo da conta: '))
deposito = float(input('Informe o valor do deposito: '))
saldo += deposito
print(f'Novo saldo: {saldo}')

#3 Orçamento e Gasto
orcamento = float(input('\nInforme o orçamento: '))
gasto = float(input('Informe o valor gasto: '))
orcamento -= gasto
print(f'Orçamento Restante: {orcamento}')

#4 Estoque do dia
estoque_inicio = int(input('\nEstoque no início do dia: '))
vendido_final = int(input('Quantidade vendida: '))
estoque_inicio -= vendido_final
print(f'Estoque Final:{estoque_inicio}')

#5 Multiplicar número por 3
numero = int(input('\nInforme um número: '))
numero *= 3
print(f'Resultado: {numero}')

#6 Converter horas para dias
horas = int(input('\nInforme quantidade de horas: '))
dias = horas // 24
print(f'Dias Inteiros: {dias}')

#7 Segundos para minutos
segundos = int(input('\nInforme os segundos: '))
minutos = segundos // 60
print(f'Minutos Inteiros: {minutos}')

#8 Par ou Ímpar
n = int(input('\nInforme um número: '))
n %= 2
print(f'Resultado: {n}')
if n == 0:
    print('O número é par')
else:
    print('O número é ímpar')

#9 Controle de Estoque
estoque = int(input('\nInforme o estoque inicial: '))
vendas = int(input('Informe a quantidade de vendas: '))
reposicao = int(input('Informe a quantidade de reposição: '))
estoque -= vendas
estoque =+ reposicao
estoque %= 6
print(f'Estoque Final: {Estoque}')

#10 Converter segundos em minutos e segundos
tempo = int(input('\nInforme o tempo em segundos: '))
minutos = tempo
minutos //= 60
segundosrestantes = tempo
segundosrestantes %= 60
print(f'Minutos: {minutos}')
print(f'Segundos restantes: {segundosrestantes}')

#11 Converter metros para km e metros restantes
metros = int(input('\nInforme a distância em metros: '))
km = metros
km //= 1000
metrosrestantes = metros
metrosrestantes = %= 1000
print(f'Km: {km}')
print(f'Metros restantes: {metrosrestantes}')